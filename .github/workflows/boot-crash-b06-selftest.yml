name: boot-crash B06 checker self-test

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  selftest:
    # This repository currently has no budget reserved for GitHub-hosted
    # runners. Keep this workflow opt-in and require a project-controlled
    # self-hosted Linux/x86-64 runner instead of silently consuming hosted
    # runner minutes.
    runs-on: [self-hosted, linux, x64, kernel-course]
    timeout-minutes: 30
    steps:
      - name: Checkout course repository
        uses: actions/checkout@v4
        with:
          clean: true
          fetch-depth: 1
          persist-credentials: false

      - name: Record course test provenance
        run: |
          test -z "$(git status --porcelain)"
          git show -s --format='course HEAD=%H %s' HEAD
          printf 'checker blob='
          git hash-object boot-crash/labs/06-kexec-lifecycle/verify_source_contract.py
          printf 'fixture blob='
          git hash-object boot-crash/labs/06-kexec-lifecycle/test_verify_source_contract.py
          python3 --version

      - name: Run exact committed B06 fixture suite
        working-directory: boot-crash/labs/06-kexec-lifecycle
        run: python3 -m unittest -v test_verify_source_contract.py

      # Keep the large upstream tree outside the course checkout. Otherwise a
      # failed run can leave linux-v5.10/ as an untracked directory inside the
      # course worktree, weakening the clean-tree provenance boundary until a
      # later checkout happens to remove it.
      - name: Prepare isolated upstream workspace
        shell: bash
        run: |
          upstream_dir="${RUNNER_TEMP}/kernel-course-linux-v5.10-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"
          rm -rf -- "$upstream_dir"
          printf 'B06_UPSTREAM_DIR=%s\n' "$upstream_dir" >> "$GITHUB_ENV"

      - name: Checkout upstream Linux v5.10
        uses: actions/checkout@v4
        with:
          repository: torvalds/linux
          ref: 2c85ebc57b3e1817b6ce1a6b703928e113a90442
          path: ${{ env.B06_UPSTREAM_DIR }}
          fetch-depth: 1
          clean: true
          persist-credentials: false

      - name: Verify upstream checkout identity
        shell: bash
        run: |
          test "$(git -C "$B06_UPSTREAM_DIR" rev-parse HEAD)" = "2c85ebc57b3e1817b6ce1a6b703928e113a90442"
          test -z "$(git -C "$B06_UPSTREAM_DIR" status --porcelain)"
          git -C "$B06_UPSTREAM_DIR" show -s --format='upstream HEAD=%H %s' HEAD

      - name: Verify upstream Linux v5.10 source contracts
        run: python3 boot-crash/labs/06-kexec-lifecycle/verify_source_contract.py "$B06_UPSTREAM_DIR"

      - name: Verify course checkout remained clean
        if: ${{ always() }}
        shell: bash
        run: test -z "$(git status --porcelain)"

      # A persistent self-hosted runner reuses its work directory. Delete the
      # isolated upstream tree after success or failure so repeated validation
      # cannot accumulate Linux source trees in RUNNER_TEMP.
      - name: Clean isolated upstream checkout
        if: ${{ always() }}
        shell: bash
        run: |
          if [[ -n "${B06_UPSTREAM_DIR:-}" && -e "$B06_UPSTREAM_DIR" ]]; then
            rm -rf -- "$B06_UPSTREAM_DIR"
          fi
          [[ -z "${B06_UPSTREAM_DIR:-}" || ! -e "$B06_UPSTREAM_DIR" ]]
