# Linux Kernel 5.10 课程内容编写规则

本文件用于指导本仓库后续课程内容的生成、修改和维护。课程正文、课程大纲和学习材料放在各领域的 `README.md`、`docs/`、`labs/` 与 `source-paths/` 中；编写规则和组织原则统一放在本文件中。

## 1. 课程范围

当前阶段聚焦 Linux kernel 5.10 最基础、最重要的运行机制：

```text
x86-64 汇编与内核入口
x86_64 启动、Kexec、Kdump 和 vmcore
内存管理
时间与定时器
任务调度
跨领域基础执行过程
```

VFS、具体文件系统、cgroup、namespace、网络协议栈、eBPF、设备模型和安全子系统等内容不纳入当前基础课程。遇到这些概念时，只补充理解当前机制所需的背景，不展开完整专题。

主要源码和实验基线：

```text
内核版本：Linux kernel 5.10
主要架构：x86-64
汇编器：GNU assembler
主要汇编语法：AT&T
用户态 ABI：System V AMD64 ABI
```

涉及其他版本时，先以 Linux 5.10 说明机制，再单独列出主要差异。不要把不同版本的实现混写成同一条调用路径。

## 2. 目录组织

遵循以下原则：

```text
一个领域一个目录
一个目录一个大纲
```

具体要求：

- 每个领域在仓库根目录下使用一个独立目录；
- 每个领域的课程大纲只放在该目录的 `README.md` 中；
- 根目录 `README.md` 只介绍课程范围、领域关系和推荐学习顺序；
- `docs/` 只放具体章节，不再建立第二份课程总纲；
- `labs/` 只放实验代码、构建脚本、调试脚本和实验说明；
- `source-paths/` 只放源码入口、调用路径、关键数据结构和源码阅读记录；
- 不为尚未产生内容的目录创建占位文件；
- 不在多个领域重复保存同一份正文。

推荐结构：

```text
领域目录/
├── README.md
├── docs/
├── labs/
└── source-paths/
```

## 3. README 与其他文件的职责

### 根目录 README

只保留：

- 本阶段学习范围；
- 各领域的关系；
- 推荐学习顺序；
- 暂不展开的内容；
- 源码和实验基线。

不要在根 README 中写内容生成规则、章节模板、维护流程或文风要求。

### 领域 README

作为该领域唯一的课程大纲，主要包括：

- 领域要解决的问题；
- 学习目标；
- 课程主线；
- 章节列表及每章核心内容；
- 已完成章节和实验的链接；
- 推荐源码入口；
- 与其他基础领域的必要关系。

### docs

每个文件只讲一个明确主题。章节应能独立阅读，同时通过链接连接前置知识和后续内容。

### labs

实验必须服务于正文中的具体结论。每个实验应说明：

- 要验证的问题；
- 构建和运行方法；
- 观察点；
- 预期结果；
- 结果为何如此；
- 环境限制或未完成项。

### source-paths

源码分析文件应记录：

- Linux 5.10 中的源码位置；
- 入口函数或入口符号；
- 主要调用路径；
- 关键数据结构及字段；
- 执行上下文；
- 锁、抢占、中断和内存分配约束；
- 与正文对应的章节链接。

## 4. 讲解顺序

每一章原则上按下面的顺序展开：

```text
问题背景
→ 基本原理
→ 设计考虑
→ 关键概念和数据结构
→ Linux 5.10 源码位置
→ 主要执行过程
→ 实验或观测
→ 边界条件和常见误区
→ 必要的关联知识
```

具体要求：

1. 先说明机制为什么存在，再进入源码；
2. 先建立完整的工作模型，再分析函数和字段；
3. 先讲正常主线，再讲异常路径、优化和边界情况；
4. 不要用长调用链代替原理解释；
5. 不要只罗列结构体字段，应说明字段在过程中的作用；
6. 关联知识只补充当前问题所需部分，不改变章节主线；
7. 对汇编内容，应同时说明寄存器、内存、栈、标志位和控制流的变化；
8. 对并发内容，应说明执行上下文、可否睡眠、抢占状态、锁和对象生命周期；
9. 对启动和 Kdump 内容，应明确区分生产内核、捕获内核、保留内存和旧内核内存。

## 5. 文风

统一使用平实、准确的教材语气。

- 使用完整句子说明因果关系；
- 少用口号式、宣传式和项目管理式措辞；
- 首次出现术语时先解释，再使用缩写；
- 不用术语堆砌代替说明；
- 示例从简单情况开始，再逐步增加条件；
- 标题应直接说明本节内容；
- 中文为主，必要的函数名、结构体名和指令保留英文；
- 同一概念在不同章节中使用一致名称；
- 不把推测写成确定结论；
- 尚未验证的实验结果应明确说明。

推荐表达：

> `cmp` 会进行一次减法，但不会保存减法结果。它只根据结果更新标志位，后续条件指令再读取这些标志位。

避免表达：

> `cmp` 是连接数据世界与控制流世界的关键桥梁。

## 6. 源码分析规则

分析 Linux 5.10 源码时：

1. 先交代问题背景和基本原理；
2. 给出准确的源码路径；
3. 确认入口函数、入口符号或宏的定义位置；
4. 沿主要调用路径逐步分析，不一次展开所有分支；
5. 说明关键参数从哪里来、返回值到哪里去；
6. 说明关键对象由谁创建、持有、修改和释放；
7. 说明当前处于进程、硬中断、软中断、NMI 或早期启动上下文；
8. 区分架构规定、内核设计和具体版本实现；
9. 对汇编宏，应展开到足以理解控制流和栈布局的程度；
10. 对版本差异，单独放在“版本差异”小节，不打断 Linux 5.10 主线。

引用源码时优先使用：

```text
文件路径
函数或符号名
关键结构体
简化后的调用路径
必要的短代码片段
```

不要大段复制内核源码。正文应以解释为主。

## 7. 实验与验证

新增实验时应尽量提供：

```text
README.md
示例源码
Makefile 或明确构建命令
GDB、ftrace、perf 或 crash 操作步骤
expected-analysis.md
```

验证要求：

- 能本地编译的代码应实际编译；
- 能运行的实验应实际运行并记录结果；
- 汇编实验应检查 AT&T 和 Intel 反汇编中的关键指令；
- 需要符号定位时使用 `nm`、`readelf` 或 `objdump` 验证；
- 当前环境缺少 GDB、虚拟机或 crash 时，应说明脚本已检查但未执行；
- 不用 shell 退出码代表完整寄存器值，除非正文明确说明其截断规则；
- 破坏性实验，如触发 panic，必须标明只能在隔离环境中进行。

## 8. 跨领域内容

同一机制只在一个领域中完整讲解，其他领域通过链接引用。

例如：

- 缺页入口的汇编部分放在 `assembly/`，缺页处理主体放在 `memory/`；
- `switch_to` 的汇编动作放在 `assembly/`，调度决策放在 `scheduler/`；
- 正常启动、Kexec 和 Kdump 主体放在 `boot-crash/`；
- 完整的跨领域执行过程放在 `integrated-paths/`。

跨领域章节应重点说明交接点：

```text
上一阶段留下了什么状态？
下一阶段接收了什么参数或对象？
执行上下文是否发生变化？
CPU、栈、页表或任务状态是否发生变化？
```

## 9. 修改现有内容时的检查顺序

修改或新增课程内容前：

1. 阅读根目录 `AGENTS.md`；
2. 阅读对应领域的 `README.md`；
3. 检查是否已有相同主题；
4. 确认内容应放在 README、docs、labs 还是 source-paths；
5. 确认是否需要更新领域大纲；
6. 完成内容后检查链接、文件名、章节编号和实验入口；
7. 对代码和命令进行能够完成的实际验证。

## 10. 完成标准

一章内容至少应使读者能够回答：

```text
这个机制解决什么问题？
它的基本模型是什么？
为什么采用这种设计？
Linux 5.10 的入口在哪里？
关键数据结构是什么？
主要过程如何运行？
在哪种执行上下文中运行？
如何通过实验或工具验证？
有哪些重要限制和常见误区？
```

不能回答这些问题时，说明章节仍停留在概念罗列或调用链摘录阶段，需要继续补充。